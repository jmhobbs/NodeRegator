-# views/layout.html.haml
%html
  %head
    %title= title
    %link{rel: 'stylesheet', href: '/style.css'}
  %body
    #wrapper
      %h1= title
      - if (flashes)
        %ul.messages.info
          - each msg in flashes
            %li= msg
      .body!= body